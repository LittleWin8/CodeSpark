<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.littlewin.codespark.mapper.UserTokenUsageMapper">

    <!-- 原子累加，返回影响行数；0 = 当月首条，由 Service 决定插入 -->
    <update id="incrTokens">
        UPDATE user_token_usage
        SET "inputTokens"  = "inputTokens" + #{input},
            "outputTokens" = "outputTokens" + #{output},
            "totalTokens"  = "totalTokens" + #{total}
        WHERE "userId" = #{userId}
          AND "modelName" = #{modelName}
          AND "month" = #{month}
    </update>

    <select id="sumTokensByUserId" resultType="java.lang.Long">
        SELECT COALESCE(SUM("totalTokens"), 0)
        FROM user_token_usage
        WHERE "userId" = #{userId}
    </select>

    <select id="sumTokensByUserIds" resultType="top.littlewin.codespark.model.vo.UserTokenSumVO">
        SELECT "userId" AS userId, SUM("totalTokens") AS totalTokens
        FROM user_token_usage
        WHERE "userId" IN
        <foreach collection="userIds" item="uid" open="(" separator="," close=")">
            #{uid}
        </foreach>
        GROUP BY "userId"
    </select>

</mapper>
